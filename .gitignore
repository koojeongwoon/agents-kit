# OS
.DS_Store
Thumbs.db

# Editor / swap
*~
*.swp
*.swo
.idea/

# Node / frontend build output
node_modules/
dist/

# Rust / Tauri build output
target/

# setup-symlinks.sh 백업 (기존 파일 덮어쓸 때 생성)
*.bak

# 로컬 전용 override (공용 repo에 올리지 않음)
*.local.md
*.local.toml
*.override.md
AGENTS.override.md

# 시크릿 (혹시 adapter에 실수로 넣는 경우 방지)
.env
.env.*
*.pem
credentials.json

# 루프 실행 이력 — 로컬 only로 두려면 아래 주석 해제
# loops/*/memory.md
